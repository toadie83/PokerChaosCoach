The 1.8 BB image and mask come from the user's exported local OCR trial.
Only the isolated numeric label is retained, with no player or table identity.
Amount and suffix PNGs are crops produced by the column-gap split plus 12-pixel
white padding. digits-88.png repeats the actual digit 8 from the numeric amount
as a negative suffix control. These fixtures reproduce the original 1.888 error.
